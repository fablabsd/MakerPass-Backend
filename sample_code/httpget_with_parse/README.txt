

This program is just the httpget program which calls a web service that returns XML
mixed with the DOMParser program to parse it out searching for a specific field in the output.
You can see the section I injected into the code of DOMParser stuff right after the httpget is complete
